geom.pos.mean <- function(x) {
#  numValues <- 0
#  product <- 1
#  for (value in x) {
#    if (x > 0) {
#      numValues <- numValues + 1
#      product <- prod(product, x)
#    }
#  }
#  return(product**(1 / numValues))
#}



neg.mean <- function
